# Correo en Railway — MGF Workspace
#
# Producción: https://mgfapp.up.railway.app
# Guía completa: docs/correo-produccion.md
#
# Railway BLOQUEA SMTP saliente (Gmail 465/587 → Connection timed out).
# Solución recomendada (gratis): Brevo + Gmail verificado como remitente.
# Local: sigue usando Gmail SMTP.

# =============================================================================
# RAILWAY (producción) — Brevo + Gmail verificado
# =============================================================================
#
# 1. https://www.brevo.com → cuenta gratis
# 2. Remitentes → Agregar remitente → brayanalmengorz@gmail.com → Verificado
# 3. SMTP y API → Claves API → Generar → copia xkeysib-...
# 4. En Railway → Variables:

MAIL_MAILER=brevo
BREVO_API_KEY=xkeysib-tu_clave_aqui
MAIL_FROM_ADDRESS=brayanalmengorz@gmail.com
MAIL_FROM_NAME=MGF Workspace
APP_URL=https://mgfapp.up.railway.app
QUEUE_CONNECTION=sync

# Quita o deja sin usar: MAIL_HOST, MAIL_PASSWORD, MAIL_PORT, MAIL_SCHEME, MAIL_USERNAME, RESEND_API_KEY

# =============================================================================
# LOCAL (desarrollo) — Gmail SMTP
# =============================================================================
#
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=465
# MAIL_SCHEME=smtps
# MAIL_USERNAME=tu_correo@gmail.com
# MAIL_PASSWORD=contraseña_aplicacion_google
# MAIL_FROM_ADDRESS=tu_correo@gmail.com
# MAIL_FROM_NAME=MGF Workspace

# Diagnóstico: just prod-mail-diagnose
# Logs Railway: mail-probe: Brevo API configurada (HTTPS — funciona en Railway).

# =============================================================================
# Alternativa: Resend (requiere dominio propio verificado para cualquier destinatario)
# =============================================================================
#
# MAIL_MAILER=resend
# RESEND_API_KEY=re_...
# MAIL_FROM_ADDRESS=invitaciones@tudominio.com
