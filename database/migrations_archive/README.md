# Migraciones archivadas (pre-squash)

Copia de las 22 migraciones originales antes del squash a `database/schema/mysql-schema.sql`.

No se ejecutan. Solo referencia histórica del esquema incremental.
